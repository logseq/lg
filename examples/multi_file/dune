(rule
 (target app.ml)
 (deps math.cljc main.cljc)
 (action
  (run %{exe:../../bin/lg_cli.exe} --compile-files %{deps} -o %{target})))

(executable
 (name app)
 (modules app)
 (libraries lg rrbvec core))
