(library
 (name lg)
 (public_name lg)
 ; The compiler is itself on the cold code-generation path. Classic inlining
 ; without cross-function expansion keeps that path below the 10-second gate.
 (ocamlopt_flags :standard -Oclassic -inline 0 -no-g)
 (libraries compiler-libs.common lg_compiler_support unix))
